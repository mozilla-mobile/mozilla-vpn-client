# Auto generated by: ${CMAKE_CURRENT_FUNCTION_LIST_FILE}
[target.${CONFIG_ARCH}]
linker="${CONFIG_LINKER}"

[env]
CC_${CONFIG_ARCH}="${CMAKE_C_COMPILER}"
CXX_${CONFIG_ARCH}="${CMAKE_CXX_COMPILER}"
AR_${CONFIG_ARCH}="${CMAKE_AR}"
CFLAGS_${CONFIG_ARCH}="${CMAKE_C_FLAGS}"
CXXFLAGS_${CONFIG_ARCH}="${CMAKE_C_FLAGS}"
