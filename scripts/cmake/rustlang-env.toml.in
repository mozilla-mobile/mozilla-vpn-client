CC_${RUST_CONFIG_ARCH}="${CMAKE_C_COMPILER}"
CXX_${RUST_CONFIG_ARCH}="${CMAKE_CXX_COMPILER}"
AR_${RUST_CONFIG_ARCH}="${CMAKE_AR}"
CFLAGS_${RUST_CONFIG_ARCH}="${CMAKE_C_FLAGS}"
CXXFLAGS_${RUST_CONFIG_ARCH}="${CMAKE_CXX_FLAGS}"
