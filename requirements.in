aqtinstall==3.3.0
glean-parser==18.1.1
oathtool==2.4.0
pyrsistent==0.20.0
yamllint==1.37.1

# Windows-only dependency included via glean-parser.
# Dependabot misses this, so let's make it explict.
colorama==0.4.6
